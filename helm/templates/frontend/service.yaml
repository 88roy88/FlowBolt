apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-builder.fullname" . }}-frontend
  labels:
    {{- include "ai-builder.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  selector:
    {{- include "ai-builder.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
