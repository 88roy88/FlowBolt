{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ai-builder.fullname" . }}
  labels:
    {{- include "ai-builder.labels" . | nindent 4 }}
{{- end }}
