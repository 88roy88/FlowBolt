### react-hook-form — forms
- react-hook-form is selected: plan forms around useForm, registered fields, register validation rules, and inline error states.
- Keep simple search/filter inputs outside react-hook-form unless they are part of a submitted form.
