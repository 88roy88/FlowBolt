- `react-hook-form` is selected: plan form components around `useForm`, registered fields, validation rules, and visible error states.
- Keep simple search/filter inputs outside React Hook Form unless they are part of a submitted form.
