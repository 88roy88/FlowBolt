Required:
- Keep form fields registered through `react-hook-form`.
- Read validation errors from `formState.errors`.
- Keep submit handlers wrapped with `handleSubmit`.

Forbidden:
- Do not replace React Hook Form with manual state unless the selected package is removed.
- Do not add resolver packages such as Zod or Yup unless they are already installed.
