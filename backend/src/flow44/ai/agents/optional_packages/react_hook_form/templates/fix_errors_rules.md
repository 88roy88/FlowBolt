### react-hook-form — forms
- Keep fields registered through react-hook-form; read errors as errors.field?.message.
- Align the useForm<FormValues>() type with the registered field names; validate with register rules rather than adding zod/yup.
