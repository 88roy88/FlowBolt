### lucide-react — icons
- lucide-react is already installed — add new icons from it rather than inline SVG.
- Add each icon to the file's existing `from 'lucide-react'` import, using its exact PascalCase name.
- Match the sizing and color classes the surrounding icons use, and keep a text label or aria-label alongside.
