### recharts — charts
- Blank chart? Wrap it in <ResponsiveContainer width="100%" height={300}> — it needs an explicit height.
- Keep each series' dataKey matching a key in the data objects; set series colors via stroke/fill props.
