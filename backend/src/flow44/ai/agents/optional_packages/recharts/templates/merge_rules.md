### recharts — charts
- recharts is selected: plan focused chart components plus any transformation of data-source rows into an array of objects.
- Match the chart type to the request (trend → line/area, comparison → bar, composition → pie).
- Use recharts for charts only — not for maps or node/relationship diagrams.
