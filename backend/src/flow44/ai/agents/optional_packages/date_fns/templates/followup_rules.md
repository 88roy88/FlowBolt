### date-fns — dates
- date-fns is already installed — use it for new date formatting or math rather than hand-written helpers.
- Add named helpers to the file's existing `from 'date-fns'` import rather than creating a second import.
- Match the format tokens already used in the file so new dates render consistently with the existing ones.
