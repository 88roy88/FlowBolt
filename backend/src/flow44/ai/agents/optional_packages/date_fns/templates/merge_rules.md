### date-fns — dates
- date-fns is selected: plan date formatting, relative-time labels, sorting, ranges, and date math with named helper imports.
- Normalize incoming date strings (parse to Date) before comparing or displaying them.
