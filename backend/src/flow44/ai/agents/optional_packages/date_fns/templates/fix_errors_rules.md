### date-fns — dates
- Keep date-fns imports as named helpers; parse strings with parseISO before formatting or comparing.
- If a formatted date looks wrong, replace the token with a valid date-fns format token (e.g. yyyy-MM-dd).
