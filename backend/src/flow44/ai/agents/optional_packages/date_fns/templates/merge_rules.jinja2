- `date-fns` is selected: plan date formatting, relative time labels, sorting, ranges, and date math with explicit helper imports.
- Normalize incoming date strings before comparing or displaying them.
