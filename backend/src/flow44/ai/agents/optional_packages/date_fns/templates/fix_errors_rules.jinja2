Required:
- Keep `date-fns` imports as named helper imports.
- Parse string dates before formatting, sorting, or comparing them.
- Replace invalid format tokens with valid `date-fns` format tokens.

Forbidden:
- Do not replace `date-fns` with another date package.
- Do not import all helpers as a namespace.
