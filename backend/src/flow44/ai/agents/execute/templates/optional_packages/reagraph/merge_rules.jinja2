- `reagraph` is selected: create focused tasks for graph data shaping, the Reagraph visualization component, and any controls or detail panels around node/edge selection.
- Keep Reagraph rendering in a dedicated component when the app has non-trivial graph controls, filters, selection state, or detail panels.
- Ensure at least one task defines stable node ids, edge source/target ids, and the data-to-Reagraph mapping before the graph component consumes it.
- Do not create dependency-install tasks.
- `reagraph` is selected: create focused tasks for graph data shaping, the Reagraph visualization component, and any surrounding controls or detail panels. Do not create dependency-install tasks.
- Keep Reagraph rendering in a dedicated component when the app has non-trivial filtering, selection, layout switching, or detail panels.
- Ensure at least one task defines stable node/edge ids and the data-to-Reagraph mapping before the graph task consumes it.
