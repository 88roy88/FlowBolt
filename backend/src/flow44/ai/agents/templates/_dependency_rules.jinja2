## Dependency Rules

Optional packages are selected before this prompt runs. During initial builds,
PlanAgent selects them before ExecuteAgent writes code. During follow-up edits,
FollowUpAgent may select additional packages before editing.

Pre-configured (no import needed):
- Tailwind CSS — pre-configured; style with utility classes.

Allowed imports:
- React / React DOM
- Browser APIs
- Local project files
{% for package in allowed_imports %}
- {{ package }}
{% endfor %}

Forbidden:
- Do not import unselected packages.
- Do not edit `package.json`.
- Do not install dependencies.
- Do not use extra npm packages unless they are already selected and listed above.
