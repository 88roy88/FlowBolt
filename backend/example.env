
# OpenRouter API Key and Model

AIB_AI_MODEL="openrouter/minimax/minimax-m2.5"
OPENROUTER_API_KEY="sk-or-v1-..."

# Langfuse (optional — for observability/tracing)
# Browser auth blob key for AI/codegen prompts (match frontend VITE_AUTH_STORAGE_KEY default).
# AIB_AUTH_STORAGE_KEY=Auth

# AIB_LANGFUSE_PUBLIC_KEY="pk-lf-..."
# AIB_LANGFUSE_SECRET_KEY="sk-lf-..."
# AIB_LANGFUSE_HOST="https://cloud.langfuse.com"

# Flapi upstream (data-source search / run)
# AIB_FLAPI_BASE_URL=http://localhost:4000
# When true (default), raw SSO tokens from the browser are sent upstream as "Bearer <token>".
# Set to false only if your Flapi expects a non-Bearer Authorization value verbatim.
# AIB_FLAPI_ADD_BEARER_PREFIX=true

# For OpenAI APis like 80:
OPENAI_API_KEY="default"  # This is a dummy value to prevent errors.
OPENAI_HOST="https://something.com"  # This is a dummy value to prevent errors.

# for lite llm not trying to fetch cost from online
LITELLM_LOCAL_MODEL_COST_MAP="true"